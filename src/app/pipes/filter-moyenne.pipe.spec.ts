import { FilterMoyennePipe } from './filter-moyenne.pipe';

describe('FilterMoyennePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMoyennePipe();
    expect(pipe).toBeTruthy();
  });
});
